# Manage symlink with expected config location

```bash 
# Create a symlink back at the expected location
ln -s ~/Projects/hammerspoon ~/.hammerspoon

# Confirm symlink is in place
ls -l ~/.hammerspoon
```